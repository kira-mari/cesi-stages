# ================================================
# Configuration CesiStages
# ================================================
# Copiez ce fichier vers .env et modifiez les valeurs
# cp .env.example .env
# ================================================

# ------------------------------------------------
# BASE DE DONNÉES
# ------------------------------------------------
DB_HOST=localhost
DB_NAME=cesi_stages
DB_USER=root
DB_PASS=

# ------------------------------------------------
# GOOGLE OAUTH (SSO) - Optionnel
# ------------------------------------------------
# Pour activer la connexion Google, créez un projet sur
# https://console.cloud.google.com/
# et configurez les credentials OAuth 2.0
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ------------------------------------------------
# EMAIL (SMTP) - Optionnel
# ------------------------------------------------
# Pour envoyer des emails (vérification, mot de passe oublié)
# Vous pouvez utiliser Brevo (ex-Sendinblue), Gmail, etc.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=no-reply@cesistages.fr
SMTP_FROM_NAME=CESI Stages
